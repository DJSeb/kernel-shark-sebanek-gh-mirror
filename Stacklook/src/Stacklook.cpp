/**
 * For internal logic.
*/

#include "stacklook.h"

/**
 * Steal from public footage again :D
 * For me: Do look at the footage again to check the loot.
*/
// // C++
// #include <vector>
// // KernelShark
// #include "KsPlotTools.hpp"
// #include "KsPlugins.hpp"
// // KernelShark examples
// #include "priority_plot.h"
// using namespace KsPlot;
// static PlotObject *makeShape(std::vector<const Graph *> graph, std::vector<int> bin, std::vector<kshark_data_field_int64 *> data, Color col, float size)
// {
// int x, y;
// x = graph[0]->bin(bin[@])._val.x()
// FONT_SIZE *
// .75;
// y = graph[0]->bin(bin[0])._val.y() FONT_SIZE / 3;
// TextBox *tb = new TextBox(get_font_ptr(), std::to_string(data[0]->field), col, {x, y));
// return tb;
// }
// void draw_priority(struct kshark_cpp_argv *argv_c, int sd, int val, int draw_action)
// 10
// KsCppArgV *argvCpp KS_ARGV_TO_CPP(argv_c); kshark_data_container *plugin_data;
// if (!(draw_action & KSHARK_CPU_DRAW)) return;
// if (argvCpp->_histo->tot_count > 200) return;
// plugin_data = get_plugin_data(sd); if (!plugin_data) return;
// auto check = [=] (kshark_data_container *d, ssize_t t) { return (d->data[i]->entry->cpu val) ? true: false;
// };
// eventFieldPlotMin(argvCpp,
// plugin_data, check, makeShape, (0, 0, 255), // Blue 10); // Size